<h1>Editing Book</h1>

<% if @books.errors.any? %>
<div id="error">
　<h2><%= @books.errors.count %>error prohibited this book from being saved:</h2>
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form_with model:@books do |f| %>
<% if @books.errors.any? %>
  <div class="field_with_errors">
    <h4 class="field">Title</h4><br>
    <div class="field">
      <%= f.text_field :title %>
    </div>
  </div><br>

  <div class="field_with_errors">
    <h4 class="field">Body</h4><br>
    <div class="field">
      <%= f.text_area :body %>
    </div>
  </div>

<% else %>
  <h4>Title</h4><br>
  <%= f.text_field :title %><br>

  <h4>Body</h4><br>
  <%= f.text_area :body %>

<% end %>

  <div class="btn"><%= f.submit 'Update Book' %></div>
<% end %>

<%= link_to "Show" ,book_path(@books.id) %>
<%= link_to "Back" ,books_path %>
